version: "3.7"
services:
  php-apache:
    build: .
    logging:
      driver: "json-file"
    ports:
      - 8040:80
    links:
      - redis
  redis:
    image: redis
    expose:
      - 6379
  golang:
    build: ./go-app
